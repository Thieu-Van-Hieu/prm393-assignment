import 'package:flutter/material.dart';

class AppColors {
  // 1. MÀU SẮC THƯƠNG HIỆU CHÍNH (BRAND COLORS)
  static const Color orangeFPT = Color(0xFFF36F21);
  static const Color blueFPT = Color(0xFF0054A5);
  static const Color greenFPT = Color(0xFF00A651);

  // 2. MÀU SẮC CHO CHỮ (TEXT COLORS)
  static const Color textPrimary = Color(0xFF2D3748);
  static const Color textSecondary = Color(0xFF718096);
  static const Color textLabel = Color(0xFFF36F21);
  static const Color textLink = Color(0xFF718096);
  static const Color textButton = Colors.white;

  // 3. MÀU NỀN & THÀNH PHẦN GIAO DIỆN (BACKGROUND & UI ELEMENTS)
  static const Color background = Colors.white;
  static const Color scaffoldBackground = Color(0xFFF8F9FA);
  static const Color cardBackground = Colors.white;
  static const Color inputBorder = Color(0xFFE2E8F0);
  static const Color iconEye = Color(0xA3A0AEC0);
  static const Color buttonPrimary = Color(0xFFF36F21);
  static const Color buttonSecondary = Color(0xFF718096);
  static const Color divider = Color(0xFFEDF2F7);

  // 4. MÀU TRẠNG THÁI ĐIỂM DANH & HỌC TẬP (STATUS COLORS)
  static const Color statusAttendedBg = Color(0xFFE6F4EA);
  static const Color statusAttendedText = Color(0xFF72BB53);

  static const Color statusAbsentBg = Color(0xFFFCE8E6);
  static const Color statusAbsentText = Color(0xFFC5221F);

  static const Color statusPendingBg = Color(0xFFFEF3E6);
  static const Color statusPendingText = Color(0xFFB06000);

  // 5. MÀU ĐÁNH GIÁ & BẢNG ĐIỂM (GRADE COLORS)
  static const Color gradeGood = Color(0xFF48BB78);
  static const Color gradeAverage = Color(0xFFED8936);
  static const Color gradeBad = Color(0xFFE53E3E);

  // 6. MÀU ĐẶC THÚ CHO KHỐI CONTAINER (SPECIAL CONTAINER COLORS)
  static const Color homeBannerBg = Color(0xFF0B3C5D);
  static const Color overlayBackground = Color(0x80000000);
}
